{
  "name": "makeup-academy-fiji",
  "version": "1.0.1",
  "type": "module",
  "scripts": {
    "dev": "npm run generate:robots && vite",
    "build": "npm run generate:robots && vite build",
    "preview": "vite preview",
    "start": "serve dist -s",
    "generate:robots": "node scripts/generate-robots.js",
    "predev": "npm run generate:robots",
    "prebuild": "npm run generate:robots"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "serve": "^14.2.1"
  },
  "devDependencies": {
    "@types/react": "^18.2.43",
    "@types/react-dom": "^18.2.17",
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.0.8"
  }
}

